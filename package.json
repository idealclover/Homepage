{
  "name": "homepage",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "npm run inline && astro dev",
    "start": "npm run inline && astro dev",
    "build": "npm run inline && astro check && astro build && npm run fontmin && npm run csspath",
    "preview": "astro preview",
    "astro": "astro",

    "inline": "node build/inlinejs",
    "fontmin": "node build/fontmin",
    "csspath": "node build/csspath"
  },
  "dependencies": {
    "@astrojs/check": "^0.7.0",
    "@astrojs/tailwind": "^5.1.0",
    "astro": "^4.11.5",
    "daisyui": "^2.6.0",
    "tailwindcss": "^3.4.4",
    "typescript": "^5.5.3"
  },
  "devDependencies": {
    "fontmin": "^1.1.0",
    "terser": "^5.31.1"
  }
}
